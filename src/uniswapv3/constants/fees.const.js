/**
 * @fileoverview The default factory enabled fee amounts, denominated in hundredths of bips.
 */

const consts = (module.exports = {});

consts.FEES = {
  LOW: 500,
  MEDIUM: 3000,
  HIGH: 10000,
};

consts.FEE_DECIMALS = 10000;
